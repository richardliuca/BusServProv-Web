# Copy to .env for local tooling outside Docker (optional).
#
# The frontend-only MVP has no required runtime environment variables.
#
# When a backend is added later, point the browser/SSR at it here, e.g.:
# NEXT_PUBLIC_API_URL=https://api.example.com
